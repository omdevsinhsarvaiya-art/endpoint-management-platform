<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>EndpointAgent.Core.Tests</RootNamespace>
    <AssemblyName>EndpointAgent.Core.Tests</AssemblyName>
    <!-- Platform-neutral: runs on any OS, no Windows APIs touched. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\EndpointAgent.Core\EndpointAgent.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Real files in their real formats: a shortcut Windows wrote, for the parser. -->
    <None Include="Fixtures\**" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
